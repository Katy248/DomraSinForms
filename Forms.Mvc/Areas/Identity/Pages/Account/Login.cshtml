@page
@using Microsoft.AspNetCore.Mvc.Localization;
@model LoginModel
@inject IViewLocalizer Localizer
@{
    ViewData["Title"] = Localizer["Log in"];
}

<div class="row g-3 gx-5 justify-content-center">
    <div class="col-12 text-center display-4">@ViewData["Title"]</div>
    <div class="col-sm-10 col-md-8 col-xl-6 row g-3 gx-4">
        <section>
            <form id="account" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                <div class="form-floating mb-3">
                    <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                    <label asp-for="Input.Email" class="form-label">@Localizer["Email"]</label>
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>
                <div class="form-floating mb-3">
                    <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" placeholder="password" />
                    <label asp-for="Input.Password" class="form-label">@Localizer["Password"]</label>
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>
                <div class="checkbox mb-3">
                    <label asp-for="Input.RememberMe" class="form-label">
                        <input class="form-check-input" asp-for="Input.RememberMe" />
                        @Localizer["Remember me"]
                    </label>
                </div>
                <div>
                    <button id="login-submit" type="submit" class="w-100 btn btn-lg btn-primary">@Localizer["Log in"]</button>
                </div>

            </form>
        </section>
    </div>
    <div class="col-12 text-center row g-3">

        <p class="h5">
            <a id="forgot-password" asp-page="./ForgotPassword">@Localizer["Forgot your password?"]</a>
        </p>
        <p class="h5">
            <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">@Localizer["Register as a new user"]</a>
        </p>
        <p class="h5">
            <a id="resend-confirmation" asp-page="./ResendEmailConfirmation">@Localizer["Resend email confirmation"]</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

@*<div class="col-md-6 col-md-offset-2">
        <section>
            <h3>@Localizer["Use another service to log in."]</h3>
            <hr />
            @{
                if ((Model.ExternalLogins?.Count ?? 0) == 0)
                {
                    <div>
                        <p>
                            @Localizer["There are no external authentication services configured. See this <a href=\"https://go.microsoft.com/fwlink/?LinkID=532715\">article about setting up this ASP.NET application to support logging in via external services</a>."]

                        </p>
                    </div>
                }
                else
                {
                    <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="form-horizontal">
                        <div>
                            <p>
                                @foreach (var provider in Model.ExternalLogins!)
                                {
                                    <button type="submit" class="btn btn-primary" name="provider" value="@provider.Name" title="@Localizer.GetString("Log in using your {0} account", provider.DisplayName)">@provider.DisplayName</button>
                                }
                            </p>
                        </div>
                    </form>
                }
            }
        </section>
    </div>*@